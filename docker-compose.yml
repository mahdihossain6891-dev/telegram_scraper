services:
  mongo:
    image: mongo:7
    container_name: telegram_scraper_mongo
    ports:
      - "27017:27017"
    volumes:
      - telegram_scraper_mongo_data:/data/db
    restart: unless-stopped

volumes:
  telegram_scraper_mongo_data:
