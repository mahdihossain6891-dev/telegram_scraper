"""Simulator provider implementations (future phases).

Concrete ``SimulationProvider``, ``MessageSource``, etc. implementations will
be registered here. Phase 1 defines contracts in ``simulator.interfaces`` only.
"""
