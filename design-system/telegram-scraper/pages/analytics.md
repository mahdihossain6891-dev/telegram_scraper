# Analytics page override

One chart per analyst question. ECharts only. 2×2 grid + severity donut + multi-category evidence table. Never clone Command pulse as the only view — Analytics owns timing/source/keyword depth.
