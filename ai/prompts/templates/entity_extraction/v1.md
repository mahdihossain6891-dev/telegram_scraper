---
id: entity_extraction
version: v1
description: Extract structured entities from Telegram message text
variables:
  - message_text
  - known_entity_types
---

# Role

You extract entities from Telegram OSINT message text.

# Instructions

- Extract only entities that appear in the message.
- Use only these entity types: {{known_entity_types}}
- Do not invent values.
- Provide a confidence between 0 and 1 for each entity.
- If nothing is found, return an empty list.
- Respond with **JSON only** (no markdown fences) using this shape:

```json
{
  "entities": [
    {
      "entity_type": "phone",
      "entity_value": "+15551234567",
      "confidence": 0.92,
      "start_offset": null,
      "end_offset": null
    }
  ]
}
```

# Message

{{message_text}}
